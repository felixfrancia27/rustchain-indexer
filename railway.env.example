# Variables de Entorno para Railway
# Copia estos valores en el dashboard de Railway (Variables tab)
# NO incluyas las comillas ni el signo = al inicio
# Solo el nombre de la variable y su valor

# RPC Configuration (REQUERIDO)
RPC_HTTP_URL=http://216.106.182.100:32774

# Elasticsearch Configuration (REQUERIDO)
# Si Elasticsearch está en Railway, usa la URL pública o interna
# Ejemplo con Railway Elasticsearch: https://tu-elasticsearch.railway.app:9200
# O si está en el mismo proyecto: http://elasticsearch:9200
ES_URL=https://tu-elasticsearch.railway.app:9200

# Elasticsearch Authentication (OPCIONAL pero recomendado)
ES_USERNAME=elastic
ES_PASSWORD=tu_password_aqui

# Index Configuration (OPCIONAL - default: workqueue)
INDEX_PREFIX=workqueue

# Batch Configuration (OPCIONAL - default: 1000)
BATCH_SIZE=1000

# Starting Block (OPCIONAL - default: 0 = desde genesis)
START_BLOCK=0

# Sync Interval (OPCIONAL - default: 2 segundos)
SYNC_INTERVAL_SECS=2

# Performance Configuration (OPCIONAL)
CONCURRENCY=10
ES_BULK_SIZE=100

