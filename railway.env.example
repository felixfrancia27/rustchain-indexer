# Environment Variables for Railway
# Copy these values in the Railway dashboard (Variables tab)
# DO NOT include quotes or the = sign at the beginning
# Only the variable name and its value

# RPC Configuration (REQUIRED)
RPC_HTTP_URL=http://216.106.182.100:32774

# Elasticsearch Configuration (REQUIRED)
# If Elasticsearch is on Railway, use the public or internal URL
# Example with Railway Elasticsearch: https://your-elasticsearch.railway.app:9200
# Or if in the same project: http://elasticsearch:9200
ES_URL=https://your-elasticsearch.railway.app:9200

# Elasticsearch Authentication (OPTIONAL but recommended)
ES_USERNAME=elastic
ES_PASSWORD=your_password_here

# Index Configuration (OPTIONAL - default: workqueue)
INDEX_PREFIX=workqueue

# Batch Configuration (OPTIONAL - default: 1000)
BATCH_SIZE=1000

# Starting Block (OPTIONAL - default: 0 = from genesis)
START_BLOCK=0

# Sync Interval (OPTIONAL - default: 2 seconds)
SYNC_INTERVAL_SECS=2

# Performance Configuration (OPTIONAL)
CONCURRENCY=10
ES_BULK_SIZE=100
